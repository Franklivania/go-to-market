import { Text } from "react-native";
import React from "react";
import AppContainer from "@/layout/app-container";

export default function RequestDisplay() {
  return (
    <AppContainer>
      <Text>RequestDisplay</Text>
    </AppContainer>
  );
}
